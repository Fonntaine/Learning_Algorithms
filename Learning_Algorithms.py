import datetime
import time
import numpy as np 
import pandas

#Making a vector
#vector = np.array([1, 2, 3, 4])

#Making a matrix with x and y
#matrix_xy = np.mat([[1, 2],[3, 4],[5, 6]])

#Making a matrix with x, y, and z
#matrix_xyz = np.mat([[[1, 2], [3, 4]], 
#                   
#                    [[5, 6], [7, 8]]])

#Change a vector into a matrix
#ChangeIt = np.array([1, 2, 3, 4, 5, 6, 7, 8])
#print(ChangeIt.reshape(2, 2, 2))

#Change it from 2 rows 4 columns to 4 rows 2 columns
#ChangeIt = np.mat([[1, 2, 3, 4], [5, 6, 7, 8]])
#Print(np.transpose(ChangeIt))

#Create a identity
#print(np.identity(4))

#I dont even know
#a = np.mat([[1, 2], [3, 4]])
#b = np.linalg.inv(a)
#np.allclose(np.dot(a, b), np.identity(2))


